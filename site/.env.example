# Optional. Default is /api; Vite dev server proxies /api to the local backend.
# Use this only if backend runs outside the default local setup.
# Do not put backend secrets or provider keys here: VITE_* values are bundled into browser JS.
# VITE_API_BASE_URL=http://127.0.0.1:8000/api
