export const basicsContent = [
  "## Простое объяснение",
  "",
  "Программа — это набор инструкций для компьютера. В C++ эти инструкции сначала пишутся как исходный код в файле `.cpp`, затем компилятор переводит их в исполняемую программу. IDE, например Visual Studio или редактор с терминалом, помогает открыть файлы, подсвечивает ошибки, запускает компилятор и показывает результат.",
  "",
  "Перед ООП нужно уверенно понимать базу: переменные, типы, ввод, вывод, условия, циклы и функции. Поле класса — это переменная внутри объекта. Метод класса — это функция внутри объекта. Меню в больших задачах — это цикл `while` плюс `if` или `switch`. Поэтому база C++ не отдельная тема, а фундамент для всего курса.",
  "",
  "## Зачем это нужно",
  "",
  "Если не понимать, что такое `main`, `cout`, `cin`, `if` и `for`, классы будут казаться магией. На самом деле ООП строится из уже знакомых элементов: данные хранятся в переменных, действия оформляются функциями, выбор делается условиями, повторение делается циклами.",
  "",
  "## Когда использовать",
  "",
  "- `cout` — когда нужно вывести текст или значение.",
  "- `cin` — когда нужно считать значение от пользователя.",
  "- `int` — для целых чисел.",
  "- `double` — для дробных чисел.",
  "- `string` — для текста.",
  "- `bool` — для ответа да/нет: `true` или `false`.",
  "- `if` — когда программа должна выбрать вариант.",
  "- `for` — когда заранее понятно, сколько раз повторять действие.",
  "- `while` — когда повторение зависит от условия, например меню работает до выбора выхода.",
  "- массив — когда нужно хранить несколько значений одного типа, но размер известен заранее.",
  "",
  "## Синтаксис",
  "",
  "```cpp",
  "#include <iostream>",
  "",
  "using namespace std;",
  "",
  "int main() {",
  "    return 0;",
  "}",
  "```",
  "",
  "## Как читать код",
  "",
  "`#include <iostream>` подключает библиотеку ввода-вывода. Библиотека — это готовый набор возможностей. В `<iostream>` находятся `cout` и `cin`.",
  "",
  "`using namespace std;` позволяет писать `cout`, `cin`, `string` без приставки `std::`. В учебном курсе это упрощает первые примеры.",
  "",
  "`int main()` — главная функция. С неё начинается выполнение консольной программы. Фигурные скобки `{}` показывают тело функции. `return 0;` возвращает операционной системе код успешного завершения.",
  "",
  "## Как написать самому",
  "",
  "1. Создайте `.cpp` файл.",
  "2. Подключите нужные библиотеки.",
  "3. Напишите `int main()`.",
  "4. Внутри `main` объявите переменные.",
  "5. Добавьте ввод через `cin` или вывод через `cout`.",
  "6. Если есть выбор, добавьте `if`.",
  "7. Если есть повторение, добавьте `for` или `while`.",
  "8. Если логика повторяется, вынесите её в функцию.",
  "9. Компилируйте после каждого маленького шага.",
  "",
  "## Мини-пример",
  "",
  "```cpp",
  "#include <iostream>",
  "#include <string>",
  "",
  "using namespace std;",
  "",
  "int main() {",
  "    string name;",
  "    int age;",
  "",
  "    cout << \"Введите имя: \";",
  "    cin >> name;",
  "",
  "    cout << \"Введите возраст: \";",
  "    cin >> age;",
  "",
  "    if (age >= 18) {",
  "        cout << name << \" уже совершеннолетняя\" << endl;",
  "    } else {",
  "        cout << name << \" ещё несовершеннолетняя\" << endl;",
  "    }",
  "",
  "    return 0;",
  "}",
  "```",
  "",
  "## Пример из задач курса",
  "",
  "В первых упражнениях вы пишете минимальную программу, выводите текст, считываете возраст, проверяете условие и делаете простое меню. Эти мини-задачи потом повторяются в больших задачах: `Car` вводит поля через `cin`, `Worker` работает через меню, `Company` проверяет условия доступа.",
  "",
  "```cpp",
  "int choice = -1;",
  "",
  "while (choice != 0) {",
  "    cout << \"1. Показать данные\" << endl;",
  "    cout << \"0. Выход\" << endl;",
  "    cin >> choice;",
  "",
  "    if (choice == 1) {",
  "        cout << \"Данные\" << endl;",
  "    }",
  "}",
  "```",
  "",
  "## Плохой пример",
  "",
  "```cpp",
  "int main {",
  "    return 0",
  "}",
  "```",
  "",
  "Здесь после `main` нет круглых скобок, а после `return 0` нет точки с запятой.",
  "",
  "```cpp",
  "cout >> \"Hello\";",
  "```",
  "",
  "`cout` выводит данные, поэтому стрелки должны быть `<<`, а не `>>`.",
  "",
  "## Исправленный пример",
  "",
  "```cpp",
  "#include <iostream>",
  "",
  "using namespace std;",
  "",
  "int main() {",
  "    cout << \"Hello\";",
  "    return 0;",
  "}",
  "```",
  "",
  "## Как выбрать тип переменной",
  "",
  "| Что нужно хранить | Тип | Пример |",
  "|---|---|---|",
  "| целое число | `int` | `age`, `count`, `year` |",
  "| дробное число | `double` | `price`, `weight`, `distance` |",
  "| текст | `string` | `name`, `brand`, `material` |",
  "| да/нет | `bool` | `isOpen`, `hasLife`, `isGovernment` |",
  "",
  "Переменная нужна, когда программе нужно что-то запомнить: возраст пользователя, цену товара, имя планеты или выбранный пункт меню. Тип выбирают по смыслу данных. Возраст обычно целый, поэтому `int`. Цена и вес могут быть дробными, поэтому `double`. Имя, марка и материал — текст, значит `string`. Признак с ответом да/нет лучше хранить в `bool`: например `isOpen` или `isBunk`.",
  "",
  "Если сомневаетесь, задайте себе вопрос: какие значения вообще возможны? Если только `true` или `false`, нужен `bool`. Если нужно считать количество, нужен `int`. Если нужно хранить слово или фразу, нужен `string`.",
  "",
  "## Когда нужен if",
  "",
  "`if` нужен, когда программа должна выбрать один из вариантов. Например: если возраст больше или равен 18, вывести один текст, иначе другой. Без `if` программа выполняла бы все команды подряд и не могла бы принимать решения.",
  "",
  "```cpp",
  "if (age >= 18) {",
  "    cout << \"Доступ разрешён\";",
  "} else {",
  "    cout << \"Доступ запрещён\";",
  "}",
  "```",
  "",
  "## Когда нужен цикл",
  "",
  "Цикл нужен, когда одно действие повторяется несколько раз. `for` удобен, когда количество повторений известно заранее: например вывести числа от 1 до 10. `while` удобен, когда повторение зависит от условия: например меню работает, пока пользователь не выбрал выход.",
  "",
  "```cpp",
  "for (int i = 1; i <= 10; i++) {",
  "    cout << i << endl;",
  "}",
  "",
  "while (choice != 0) {",
  "    cout << \"1. Показать\\n0. Выход\";",
  "    cin >> choice;",
  "}",
  "```",
  "",
  "## Как читать ошибки новичка",
  "",
  "Если компилятор ругается, сначала смотрите не на весь длинный текст, а на строку и ближайшую конструкцию. Часто ошибка находится чуть выше строки, которую показывает компилятор.",
  "",
  "- `expected ';'` — скорее всего, забыта точка с запятой после команды или после объявления структуры/класса.",
  "- `expected '}'` — забыта закрывающая фигурная скобка.",
  "- `cin << age` — перепутан оператор ввода: для `cin` нужны стрелки `>>`, потому что данные идут в переменную.",
  "- `cout >> text` — перепутан оператор вывода: для `cout` нужны стрелки `<<`, потому что данные идут в консоль.",
  "",
  "## Частые ошибки",
  "",
  "1. Забывают `;` после команды.",
  "2. Пишут `int main` без `()`.",
  "3. Путают `cout <<` и `cin >>`.",
  "4. Используют `string`, но забывают `#include <string>`.",
  "5. Пишут код вне `main`.",
  "6. Забывают закрыть фигурную скобку.",
  "7. В цикле пишут неверное условие и получают бесконечное повторение.",
  "8. Пытаются писать классы, пока обычный ввод и вывод ещё не работают.",
  "",
  "## Мини-проверка",
  "",
  "1. Что такое исходный код?",
  "2. Что делает компилятор?",
  "3. Где начинается выполнение программы?",
  "4. Чем `int` отличается от `double`?",
  "5. Для чего нужен `bool`?",
  "6. Чем `for` отличается от `while`?",
  "7. Почему функции нужны перед изучением методов класса?",
  "",
  "## Задачи после темы",
  "",
  "- `practice/00_basics/ex01_minimal_program.cpp` — минимальный каркас программы.",
  "- `practice/00_basics/ex02_print_hello.cpp` — вывод через `cout`.",
  "- `practice/00_basics/ex03_input_age.cpp` — ввод через `cin`.",
  "- `practice/00_basics/ex04_if_age.cpp` — условие `if`.",
  "- `practice/00_basics/ex05_for_loop.cpp` — цикл `for`.",
  "- `practice/00_basics/ex06_function_square.cpp` — функция и `return`.",
  "- `practice/00_basics/ex07_simple_menu.cpp` — меню на `while`.",
].join("\n");
