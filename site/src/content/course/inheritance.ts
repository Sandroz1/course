export const inheritanceContent = [
  "## Простое объяснение",
  "",
  "Наследование нужно, когда один класс является более частным вариантом другого класса. Базовый класс описывает общую часть, а дочерний класс добавляет свои данные и методы.",
  "",
  "Например, `Body` описывает физическое тело: размеры и массу. `AreaBody` тоже физическое тело, но дополнительно хранит координаты на плоскости. Поэтому `AreaBody` можно сделать наследником `Body`.",
  "",
  "Главная запись этой темы:",
  "",
  "```cpp",
  "class AreaBody : public Body {",
  "    // дополнительные поля и методы AreaBody",
  "};",
  "```",
  "",
  "Это читается так: `AreaBody` публично наследуется от `Body`. Объект `AreaBody` содержит внутри базовую часть `Body` и свою дополнительную часть.",
  "",
  "## Родительский и дочерний класс",
  "",
  "В наследовании часто говорят:",
  "",
  "- родительский класс, базовый класс, base class — класс, от которого наследуются;",
  "- дочерний класс, производный класс, derived class — класс, который наследуется.",
  "",
  "В примере ниже `Body` — базовый класс, а `AreaBody` — производный класс:",
  "",
  "```cpp",
  "class Body {",
  "    // общие данные физического тела",
  "};",
  "",
  "class AreaBody : public Body {",
  "    // координаты тела на плоскости",
  "};",
  "```",
  "",
  "Важно не путать класс и объект. `Body` и `AreaBody` — это типы. `Body body;` и `AreaBody areaBody;` — это объекты.",
  "",
  "## Что наследуется",
  "",
  "Производный класс получает базовую часть объекта. Это значит, что у `AreaBody` есть данные и методы `Body`, но доступ к ним зависит от `public`, `protected` и `private`.",
  "",
  "Что важно для текущего уровня:",
  "",
  "- `public`-методы базового класса можно вызывать у дочернего объекта при `public` наследовании;",
  "- `protected`-члены доступны внутри базового класса и внутри наследников;",
  "- `private`-члены базового класса находятся в объекте, но напрямую недоступны в дочернем классе;",
  "- конструктор базового класса нужно вызвать из конструктора наследника через список инициализации;",
  "- деструктор базового класса вызывается автоматически после уничтожения дочерней части.",
  "",
  "Здесь мы не используем виртуальные методы. Эта тема только про базовое наследование и доступ.",
  "",
  "## public, protected и private внутри класса",
  "",
  "Модификаторы доступа внутри класса отвечают на вопрос: кто может обратиться к полю или методу.",
  "",
  "- `public` — доступно снаружи класса, например из `main()`.",
  "- `protected` — доступно внутри самого класса и внутри классов-наследников.",
  "- `private` — доступно только внутри самого класса.",
  "",
  "Обычно поля оставляют `private`, чтобы внешний код не ломал состояние объекта. `protected` используют осторожно: только если наследнику действительно нужна общая служебная часть базового класса.",
  "",
  "## Пример Body",
  "",
  "Базовый класс может хранить размеры и массу, а наружу давать метод вывода:",
  "",
  "```cpp",
  "class Body {",
  "private:",
  "    double width;",
  "    double height;",
  "    double depth;",
  "    double mass;",
  "",
  "protected:",
  "    double getVolume() const {",
  "        return width * height * depth;",
  "    }",
  "",
  "public:",
  "    Body(double width, double height, double depth, double mass)",
  "        : width(width), height(height), depth(depth), mass(mass) {",
  "    }",
  "",
  "    void printBodyInfo() const {",
  "        cout << \"Размеры: \" << width << \" x \" << height << \" x \" << depth << endl;",
  "        cout << \"Масса: \" << mass << endl;",
  "    }",
  "};",
  "```",
  "",
  "`width`, `height`, `depth` и `mass` закрыты. Наследник не должен менять их напрямую. Метод `getVolume()` находится в `protected`, поэтому наследник может использовать его в своих расчётах.",
  "",
  "## Пример AreaBody",
  "",
  "`AreaBody` добавляет координаты и вызывает конструктор `Body`:",
  "",
  "```cpp",
  "class AreaBody : public Body {",
  "private:",
  "    double x;",
  "    double y;",
  "",
  "public:",
  "    AreaBody(double width, double height, double depth, double mass, double x, double y)",
  "        : Body(width, height, depth, mass), x(x), y(y) {",
  "    }",
  "",
  "    void printAreaInfo() const {",
  "        printBodyInfo();",
  "        cout << \"Координаты: \" << x << \", \" << y << endl;",
  "        cout << \"Объём: \" << getVolume() << endl;",
  "    }",
  "};",
  "```",
  "",
  "Строка `: Body(width, height, depth, mass), x(x), y(y)` сначала создаёт базовую часть `Body`, а потом заполняет поля `AreaBody`.",
  "",
  "## Почему public-метод родителя работает у наследника",
  "",
  "Если наследование публичное, public-интерфейс базового класса остаётся доступным у дочернего объекта:",
  "",
  "```cpp",
  "AreaBody areaBody(2, 3, 4, 10, 5, 7);",
  "areaBody.printBodyInfo();",
  "areaBody.printAreaInfo();",
  "```",
  "",
  "`printBodyInfo()` объявлен в `Body`, но объект `areaBody` содержит базовую часть `Body`. Поэтому при `public` наследовании этот метод можно вызвать через дочерний объект.",
  "",
  "Это не значит, что наследник получил прямой доступ к private-полям. Метод `printBodyInfo()` сам находится внутри `Body`, поэтому он может читать private-поля `Body`. `AreaBody` напрямую к ним обратиться не может.",
  "",
  "## public, protected и private при наследовании",
  "",
  "Модификатор после двоеточия отвечает за то, как внешний код будет видеть унаследованный интерфейс.",
  "",
  "Для учебных задач почти всегда нужен такой вариант:",
  "",
  "```cpp",
  "class AreaBody : public Body {",
  "};",
  "```",
  "",
  "При `public` наследовании:",
  "",
  "- public-методы `Body` остаются public у `AreaBody`;",
  "- protected-члены `Body` остаются protected у `AreaBody`;",
  "- private-члены `Body` всё равно недоступны напрямую в `AreaBody`.",
  "",
  "Если написать `class AreaBody : Body`, то для `class` наследование по умолчанию будет `private`. Тогда public-методы `Body` не будут открытой частью `AreaBody` для внешнего кода.",
  "",
  "Поэтому в учебных задачах по наследованию лучше явно писать `public` после двоеточия.",
  "",
  "## Почему private-поля родителя недоступны напрямую",
  "",
  "Плохой пример:",
  "",
  "```cpp",
  "class AreaBody : public Body {",
  "public:",
  "    void printWidth() const {",
  "        cout << width << endl; // ошибка: width закрыт в Body",
  "    }",
  "};",
  "```",
  "",
  "Причина: `width` объявлен в `private` части `Body`. Закрытое поле доступно только методам самого `Body`.",
  "",
  "Правильные варианты:",
  "",
  "- оставить вывод размеров в public-методе `Body`, например `printBodyInfo()`;",
  "- добавить в `Body` public-геттер, если значение нужно читать снаружи;",
  "- сделать служебный метод `protected`, если он нужен только наследникам.",
  "",
  "Не нужно дублировать `width`, `height`, `depth` и `mass` в `AreaBody`. Эти данные уже есть в базовой части объекта.",
  "",
  "## Когда использовать protected",
  "",
  "`protected` стоит использовать редко и точечно. Он подходит, когда наследнику нужна внутренняя возможность базового класса, но внешний код не должен её вызывать.",
  "",
  "В примере `getVolume()` сделан `protected`: `AreaBody` может вывести объём в `printAreaInfo()`, но `main()` не вызывает `areaBody.getVolume()` напрямую.",
  "",
  "Если значение должно быть доступно пользователю класса, лучше сделать public-метод. Если значение не нужно наследнику, оставь его `private`.",
  "",
  "## Мини-пример целиком",
  "",
  "```cpp",
  "#include <iostream>",
  "",
  "using namespace std;",
  "",
  "class Body {",
  "private:",
  "    double width;",
  "    double height;",
  "    double depth;",
  "    double mass;",
  "",
  "protected:",
  "    double getVolume() const {",
  "        return width * height * depth;",
  "    }",
  "",
  "public:",
  "    Body(double width, double height, double depth, double mass)",
  "        : width(width), height(height), depth(depth), mass(mass) {",
  "    }",
  "",
  "    void printBodyInfo() const {",
  "        cout << \"Размеры: \" << width << \" x \" << height << \" x \" << depth << endl;",
  "        cout << \"Масса: \" << mass << endl;",
  "    }",
  "};",
  "",
  "class AreaBody : public Body {",
  "private:",
  "    double x;",
  "    double y;",
  "",
  "public:",
  "    AreaBody(double width, double height, double depth, double mass, double x, double y)",
  "        : Body(width, height, depth, mass), x(x), y(y) {",
  "    }",
  "",
  "    void printAreaInfo() const {",
  "        printBodyInfo();",
  "        cout << \"Координаты: \" << x << \", \" << y << endl;",
  "        cout << \"Объём: \" << getVolume() << endl;",
  "    }",
  "};",
  "",
  "int main() {",
  "    Body body(2, 3, 4, 10);",
  "    body.printBodyInfo();",
  "",
  "    AreaBody areaBody(2, 3, 4, 10, 5, 7);",
  "    areaBody.printBodyInfo();",
  "    areaBody.printAreaInfo();",
  "",
  "    return 0;",
  "}",
  "```",
  "",
  "## Как написать самому",
  "",
  "1. Опиши базовый класс `Body`.",
  "2. Добавь private-поля размеров и массы.",
  "3. Добавь конструктор `Body`.",
  "4. Добавь public-метод `printBodyInfo()`.",
  "5. Опиши `class AreaBody : public Body`.",
  "6. Добавь поля координат `x` и `y`.",
  "7. В конструкторе `AreaBody` вызови конструктор `Body`.",
  "8. Добавь метод `printAreaInfo()` и проверь вывод в `main()`.",
  "",
  "## Частые ошибки",
  "",
  "### Ошибка 1. Забыли `public` при наследовании",
  "",
  "Плохо:",
  "",
  "```cpp",
  "class AreaBody : Body {",
  "};",
  "```",
  "",
  "Для `class` это private-наследование по умолчанию. В учебной задаче нужно явно написать `class AreaBody : public Body`.",
  "",
  "### Ошибка 2. Пытаются обратиться к private-полю родителя напрямую",
  "",
  "Если `width` находится в `private` части `Body`, строка `cout << width;` внутри `AreaBody` не скомпилируется. Используй public-метод родителя или protected-метод, если он действительно нужен наследнику.",
  "",
  "### Ошибка 3. Дублируют поля родителя в дочернем классе",
  "",
  "Если в `AreaBody` снова написать `width`, `height`, `depth` и `mass`, получится два набора данных. Это ломает смысл наследования. Общие поля должны жить в `Body`.",
  "",
  "### Ошибка 4. Не вызывают конструктор родителя",
  "",
  "Если у `Body` нет конструктора без параметров, `AreaBody` обязан вызвать нужный конструктор `Body` в списке инициализации.",
  "",
  "### Ошибка 5. Путают объект и класс",
  "",
  "`AreaBody` — это класс. `areaBody` — объект. Метод вызывается у объекта: `areaBody.printAreaInfo()`.",
  "",
  "### Ошибка 6. Думают, что private-доступ наследуется как открытый",
  "",
  "Private-поля базового класса остаются закрытыми. Наследник получает базовую часть объекта, но не получает прямой доступ к private-полям.",
  "",
  "## Мини-проверка",
  "",
  "1. Что такое базовый класс?",
  "2. Что такое производный класс?",
  "3. Что означает запись `class AreaBody : public Body`?",
  "4. Почему `areaBody.printBodyInfo()` компилируется?",
  "5. Почему `AreaBody` не может напрямую читать private-поле `width` из `Body`?",
  "6. Чем `protected` отличается от `private`?",
  "7. Что произойдёт, если забыть `public` после двоеточия при наследовании от `class`?",
  "",
  "## Задачи после темы",
  "",
  "- `practice/10_inheritance/ex01_body_area_body.cpp` — классы `Body` и `AreaBody`, конструкторы и вывод информации.",
].join("\n");
