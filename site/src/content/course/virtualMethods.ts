export const virtualMethodsContent = [
  "## Простое объяснение",
  "",
  "Виртуальный метод нужен, когда программа работает с объектом через указатель или ссылку на базовый класс, но должна вызвать метод настоящего дочернего объекта.",
  "",
  "Без `virtual` C++ смотрит на тип переменной. Если переменная имеет тип `Old*`, вызовется метод `Old`, даже если внутри лежит объект `Nouveau`.",
  "",
  "С `virtual` C++ смотрит на настоящий объект. Если через `Old*` хранится `Nouveau`, вызовется метод `Nouveau`.",
  "",
  "В этой теме мы используем raw pointer только для учебного эксперимента. В следующей части раздела будет безопаснее хранить объекты через умные указатели.",
  "",
  "## Что такое виртуальный метод",
  "",
  "Виртуальный метод объявляется в базовом классе через слово `virtual`:",
  "",
  "```cpp",
  "class Old {",
  "public:",
  "    virtual void print() const {",
  "        cout << \"Old\" << endl;",
  "    }",
  "};",
  "```",
  "",
  "Теперь дочерний класс может переопределить этот метод:",
  "",
  "```cpp",
  "class Nouveau : public Old {",
  "public:",
  "    void print() const override {",
  "        cout << \"Nouveau\" << endl;",
  "    }",
  "};",
  "```",
  "",
  "`override` не делает метод виртуальным сам по себе. Он проверяет, что метод действительно переопределяет виртуальный метод базового класса. Если сигнатура не совпала, компилятор покажет ошибку.",
  "",
  "## Переопределение метода",
  "",
  "Переопределение — это когда дочерний класс даёт свою версию виртуального метода с той же сигнатурой.",
  "",
  "Сигнатура должна совпадать:",
  "",
  "- имя метода то же;",
  "- параметры те же;",
  "- `const` после скобок тоже совпадает;",
  "- тип возврата совместим.",
  "",
  "Если в базовом классе метод `void print() const`, то в дочернем тоже нужно писать `void print() const override`.",
  "",
  "## Без virtual",
  "",
  "Сначала посмотрим вариант без `virtual`:",
  "",
  "```cpp",
  "#include <iostream>",
  "",
  "using namespace std;",
  "",
  "class Old {",
  "public:",
  "    void print() const {",
  "        cout << \"Old\" << endl;",
  "    }",
  "};",
  "",
  "class Nouveau : public Old {",
  "public:",
  "    void print() const {",
  "        cout << \"Nouveau\" << endl;",
  "    }",
  "};",
  "",
  "int main() {",
  "    Old object;",
  "    Nouveau nouveau;",
  "    Old* ptr = new Nouveau;",
  "",
  "    object.print();",
  "    nouveau.print();",
  "    ptr->print();",
  "",
  "    delete ptr;",
  "    return 0;",
  "}",
  "```",
  "",
  "Здесь `ptr` имеет тип `Old*`. Поэтому `ptr->print()` вызовет `Old::print()`, хотя объект создан как `Nouveau`.",
  "",
  "## С virtual",
  "",
  "Теперь добавим `virtual` в базовый класс и `override` в дочерний:",
  "",
  "```cpp",
  "class Old {",
  "public:",
  "    virtual void print() const {",
  "        cout << \"Old\" << endl;",
  "    }",
  "",
  "    virtual ~Old() = default;",
  "};",
  "",
  "class Nouveau : public Old {",
  "public:",
  "    void print() const override {",
  "        cout << \"Nouveau\" << endl;",
  "    }",
  "};",
  "```",
  "",
  "Теперь `ptr->print()` вызовет `Nouveau::print()`. Изменилось не создание объекта, а выбор метода во время вызова через базовый указатель.",
  "",
  "## Что именно меняется после virtual",
  "",
  "Без `virtual`:",
  "",
  "- `Old object; object.print();` вызывает `Old::print()`;",
  "- `Nouveau object; object.print();` вызывает `Nouveau::print()`;",
  "- `Old* ptr = new Nouveau; ptr->print();` вызывает `Old::print()`.",
  "",
  "С `virtual`:",
  "",
  "- `Old object; object.print();` вызывает `Old::print()`;",
  "- `Nouveau object; object.print();` вызывает `Nouveau::print()`;",
  "- `Old* ptr = new Nouveau; ptr->print();` вызывает `Nouveau::print()`.",
  "",
  "Ключевой случай — вызов через базовый указатель или базовую ссылку. Именно там `virtual` нужен для полиморфизма.",
  "",
  "## Указатель и ссылка на родителя",
  "",
  "Виртуальный метод работает и через указатель, и через ссылку:",
  "",
  "```cpp",
  "void show(const Old& object) {",
  "    object.print();",
  "}",
  "",
  "int main() {",
  "    Old oldObject;",
  "    Nouveau nouveauObject;",
  "",
  "    show(oldObject);",
  "    show(nouveauObject);",
  "",
  "    return 0;",
  "}",
  "```",
  "",
  "Если `print()` виртуальный, функция `show` вызовет правильную версию метода для каждого реального объекта.",
  "",
  "## Почему это важно для полиморфизма",
  "",
  "Полиморфизм через базовый тип нужен, когда у разных дочерних классов есть общее действие, но реализация отличается.",
  "",
  "Например, `Vehicle` описывает транспорт. У `Car` и `Bus` есть метод `printInfo()`, но выводимые данные разные.",
  "",
  "```cpp",
  "class Vehicle {",
  "public:",
  "    virtual void printInfo() const {",
  "        cout << \"Транспорт\" << endl;",
  "    }",
  "",
  "    virtual ~Vehicle() = default;",
  "};",
  "```",
  "",
  "Тогда список может хранить указатели на `Vehicle`, а вызов `vehicle->printInfo()` выберет реализацию `Car` или `Bus`.",
  "",
  "## Почему деструктор базового класса делают virtual",
  "",
  "Если класс используется как базовый тип для полиморфизма, у него обычно делают виртуальный деструктор:",
  "",
  "```cpp",
  "class Vehicle {",
  "public:",
  "    virtual void printInfo() const {",
  "        cout << \"Транспорт\" << endl;",
  "    }",
  "",
  "    virtual ~Vehicle() = default;",
  "};",
  "```",
  "",
  "Причина простая: если объект дочернего класса когда-нибудь удаляют через указатель на базовый класс, должен корректно вызваться деструктор дочернего объекта, а потом базового.",
  "",
  "В этой теме `virtual ~Vehicle() = default;` — хорошая привычка для класса, который уже имеет виртуальные методы.",
  "",
  "## Гараж 1 без smart pointers",
  "",
  "До темы умных указателей можно сделать учебный гараж на обычных указателях без владения объектами. Объекты живут в `main`, а `vector<Vehicle*>` только ссылается на них:",
  "",
  "```cpp",
  "Car car(\"Toyota\", 2020, 180);",
  "Bus bus(\"Ikarus\", 40);",
  "",
  "vector<Vehicle*> garage;",
  "garage.push_back(&car);",
  "garage.push_back(&bus);",
  "",
  "for (Vehicle* vehicle : garage) {",
  "    vehicle->printInfo();",
  "}",
  "```",
  "",
  "Такой пример показывает виртуальный вызов, но не решает владение памятью. Не вызывай `delete` для указателей на объекты, созданные на стеке. В следующей теме гараж будет безопаснее через `std::unique_ptr`.",
  "",
  "## Как написать самому",
  "",
  "1. Создай базовый класс.",
  "2. Добавь метод, который должен работать по-разному в наследниках.",
  "3. В базовом классе поставь `virtual` перед методом.",
  "4. В дочернем классе напиши метод с той же сигнатурой и `override`.",
  "5. В базовом классе добавь `virtual ~Base() = default;`.",
  "6. Проверь обычный объект, дочерний объект и вызов через базовый указатель или ссылку.",
  "",
  "## Частые ошибки",
  "",
  "### Ошибка 1. Нет virtual в базовом классе",
  "",
  "Если `print()` не виртуальный в `Old`, то `Old* ptr = new Nouveau; ptr->print();` вызовет метод `Old`.",
  "",
  "### Ошибка 2. Метод в дочернем классе имеет другую сигнатуру",
  "",
  "Если в базовом классе `void print() const`, а в дочернем `void print()`, это разные методы. Добавь `override`, чтобы компилятор сразу поймал ошибку.",
  "",
  "### Ошибка 3. Забыли override",
  "",
  "`override` не обязателен для работы virtual, но он защищает от ошибок в сигнатуре. В учебных задачах используй его всегда при переопределении.",
  "",
  "### Ошибка 4. Удаление через базовый указатель без virtual destructor",
  "",
  "Если объект `Nouveau` создан через `new`, а удаляется через `Old*`, базовый класс должен иметь виртуальный деструктор.",
  "",
  "### Ошибка 5. Путают перегрузку и переопределение",
  "",
  "Перегрузка — это методы с одним именем, но разными параметрами. Переопределение — это новая версия виртуального метода базового класса в дочернем классе.",
  "",
  "## Мини-проверка",
  "",
  "1. Когда нужен `virtual`?",
  "2. Почему без `virtual` через `Old*` вызывается метод `Old`?",
  "3. Почему с `virtual` через `Old*` может вызваться метод `Nouveau`?",
  "4. Что проверяет `override`?",
  "5. Почему сигнатура метода должна совпадать?",
  "6. Зачем базовому классу с виртуальными методами виртуальный деструктор?",
  "7. Почему в учебном гараже `vector<Vehicle*>` не должен удалять объекты, созданные в `main`?",
  "",
  "## Задачи после темы",
  "",
  "- `practice/10_virtual_methods/ex01_old_nouveau_virtual.cpp` — эксперимент `Old` и `Nouveau` с `virtual`.",
  "- `practice/10_virtual_methods/ex02_garage_virtual_raw_pointers.cpp` — гараж `Vehicle`, `Car`, `Bus` без умных указателей.",
].join("\n");
