export const multifileProjectContent = [
  "## Простое объяснение",
  "",
  "Многофайловый проект — это программа, разделённая на несколько файлов. Когда класс становится большим, неудобно держать всё в одном `.cpp`. Обычно делают так: `main.cpp` запускает программу, `.hpp` хранит объявление класса, `.cpp` хранит реализацию методов.",
  "",
  "Это помогает читать проект: сначала видно, какие возможности есть у класса, а подробная реализация лежит отдельно.",
  "",
  "## Зачем это нужно",
  "",
  "Если весь код лежит в одном файле, он быстро превращается в длинное полотно. Разделение на файлы помогает находить нужное место: интерфейс класса в `Bed.hpp`, реализация в `Bed.cpp`, запуск в `main.cpp`.",
  "",
  "## Когда использовать",
  "",
  "- Класс становится длинным.",
  "- Методы хочется реализовать отдельно от объявления.",
  "- Один класс используется в нескольких `.cpp` файлах.",
  "- Нужно привыкнуть к структуре реальных C++ проектов.",
  "- Задача просит создать `main.cpp`, `Class.hpp`, `Class.cpp`.",
  "",
  "## Синтаксис",
  "",
  "```text",
  "main.cpp",
  "Book.hpp",
  "Book.cpp",
  "```",
  "",
  "## Как читать код",
  "",
  "`Book.hpp` — заголовочный файл. В нём объявляют класс: поля и прототипы методов. `Book.cpp` — файл реализации. В нём пишут тела методов через `Book::methodName`. `main.cpp` — точка входа, где создают объект и вызывают методы.",
  "",
  "`Book::print` читается как “метод `print` класса `Book`”. Оператор `::` показывает, какому классу принадлежит метод, если реализация вынесена за пределы класса.",
  "",
  "## Как написать самому",
  "",
  "1. Создайте `Class.hpp`.",
  "2. В `Class.hpp` объявите класс.",
  "3. Создайте `Class.cpp`.",
  "4. В `Class.cpp` подключите `#include \"Class.hpp\"`.",
  "5. Реализуйте методы через `ClassName::methodName`.",
  "6. В `main.cpp` подключите `Class.hpp`.",
  "7. При сборке компилируйте `main.cpp` и `Class.cpp` вместе.",
  "",
  "## Мини-пример",
  "",
  "```cpp",
  "// Book.hpp",
  "#pragma once",
  "#include <string>",
  "",
  "class Book {",
  "private:",
  "    std::string title;",
  "",
  "public:",
  "    Book(std::string title);",
  "    void print() const;",
  "};",
  "```",
  "",
  "```cpp",
  "// Book.cpp",
  "#include \"Book.hpp\"",
  "#include <iostream>",
  "",
  "Book::Book(std::string title) {",
  "    this->title = title;",
  "}",
  "",
  "void Book::print() const {",
  "    std::cout << title << std::endl;",
  "}",
  "```",
  "",
  "```cpp",
  "// main.cpp",
  "#include \"Book.hpp\"",
  "",
  "int main() {",
  "    Book book(\"C++\");",
  "    book.print();",
  "    return 0;",
  "}",
  "```",
  "",
  "## Пример из задач курса",
  "",
  "В задаче `Bed` файл `Bed.hpp` должен содержать объявление класса: поля, конструкторы, `edit`, `print`. Файл `Bed.cpp` должен содержать реализацию этих методов. `main.cpp` должен создать объект `Bed` и проверить работу.",
  "",
  "Команда сборки для простой проверки через g++:",
  "",
  "```bash",
  "g++ -std=c++17 main.cpp Book.cpp -o program",
  "```",
  "",
  "Если забыть `Book.cpp`, компилятор увидит объявления методов, но линковщик не найдёт их реализации.",
  "",
  "## Что видит компилятор и что делает линковщик",
  "",
  "В многофайловом проекте важно различать объявление и настоящий код метода.",
  "",
  "- `Bed.hpp` сообщает: такой класс и такие методы существуют.",
  "- `Bed.cpp` содержит настоящий код методов.",
  "- `main.cpp` использует класс `Bed`.",
  "- компилятор проверяет каждый `.cpp` файл отдельно.",
  "- линковщик соединяет готовые части программы в один исполняемый файл.",
  "",
  "Плохая команда сборки:",
  "",
  "```bash",
  "g++ -std=c++17 main.cpp -o program",
  "```",
  "",
  "Здесь участвует только `main.cpp`. Если `main.cpp` вызывает `bed.print()`, а код `Bed::print` лежит в `Bed.cpp`, линковщик не найдёт реализацию.",
  "",
  "Правильная команда:",
  "",
  "```bash",
  "g++ -std=c++17 main.cpp Bed.cpp -o program",
  "```",
  "",
  "## Почему возникает undefined reference",
  "",
  "`undefined reference` означает: объявление найдено, но реализация не подключена к сборке. Заголовок `Bed.hpp` сказал программе, что метод есть, но файл `Bed.cpp` с телом метода не участвовал в сборке.",
  "",
  "Если видите такую ошибку, сначала проверьте не синтаксис метода, а команду сборки: в ней должны быть и `main.cpp`, и файл реализации класса.",
  "",
  "## Плохой пример",
  "",
  "```cpp",
  "// main.cpp",
  "#include \"Bed.hpp\"",
  "#include \"Bed.cpp\"",
  "```",
  "",
  "Не нужно подключать `.cpp` файл через `#include`. Его нужно передавать компилятору при сборке.",
  "",
  "## Исправленный пример",
  "",
  "```bash",
  "g++ -std=c++17 main.cpp Bed.cpp -o bed_program",
  "```",
  "",
  "## Разбор задачи 4.2 Bed",
  "",
  "### Что писать в Bed.hpp",
  "Объявление класса `Bed`: private-поля и public-методы без больших тел.",
  "",
  "### Что писать в Bed.cpp",
  "Реализации конструкторов и методов: `Bed::Bed`, `Bed::edit`, `Bed::print`.",
  "",
  "### Что писать в main.cpp",
  "Создание объекта, вызов методов, меню или простая проверка.",
  "",
  "### На что обратить внимание",
  "Каждый `.cpp` файл компилируется отдельно, а потом результаты связываются. Поэтому реализации из `Bed.cpp` должны участвовать в сборке.",
  "",
  "## Частые ошибки",
  "",
  "1. Пишут реализацию метода в `.hpp`, хотя задача просит вынести в `.cpp`.",
  "2. Забывают `#include \"Bed.hpp\"` в `Bed.cpp`.",
  "3. Забывают `Bed::` перед методом.",
  "4. Компилируют только `main.cpp` и получают ошибку линковки.",
  "5. Подключают `.cpp` через `#include`.",
  "6. Дублируют класс в нескольких файлах.",
  "",
  "## Мини-проверка",
  "",
  "1. Что хранится в `.hpp`?",
  "2. Что хранится в `.cpp` реализации?",
  "3. Зачем нужен `ClassName::methodName`?",
  "4. Почему при сборке нужен не только `main.cpp`?",
  "5. Почему не стоит подключать `.cpp` через `#include`?",
  "",
  "## Задачи после темы",
  "",
  "- `practice/05_multifile_project/ex01_book` — многофайловый `Book`.",
  "- `practice/05_multifile_project/ex02_bed_training` — тренировка `Bed`.",
  "- `practice/big_tasks/task4_1_bed_one_file.cpp` — методы вне класса в одном файле.",
  "- `practice/big_tasks/task4_2_bed` — `Bed` в трёх файлах.",
].join("\n");
