export const encapsulationContent = [
  "## Простое объяснение",
  "",
  "Инкапсуляция — это идея: объект сам защищает свои данные и не разрешает внешнему коду менять их как угодно. Поля обычно делают `private`, а доступ к ним дают через методы: геттеры и сеттеры.",
  "",
  "Простыми словами: не каждый, кто пользуется объектом, должен иметь ключи от всех его внутренних данных. Пользователь класса должен видеть понятные действия, а не внутреннюю кухню.",
  "",
  "## Зачем это нужно",
  "",
  "Если поле открыто, в него можно записать бессмысленное значение. Возраст может стать `-10`, радиус планеты — отрицательным, id работника — измениться после создания, доход компании — показаться там, где доступ запрещён.",
  "",
  "Сеттер помогает проверить данные перед записью. Геттер помогает контролировать чтение. Иногда геттер просто возвращает поле, а иногда проверяет правило доступа и запрещает чтение.",
  "",
  "## Когда использовать",
  "",
  "- Поле нельзя менять напрямую.",
  "- Значение нужно проверять перед записью.",
  "- Некоторые данные можно только читать, но нельзя менять.",
  "- Доступ к данным зависит от условий.",
  "- В задаче нужны геттеры, сеттеры или `explicit`.",
  "- Класс должен отвечать за собственную корректность.",
  "",
  "## Синтаксис",
  "",
  "```cpp",
  "class Student {",
  "private:",
  "    int age;",
  "",
  "public:",
  "    void setAge(int newAge) {",
  "        if (newAge >= 0) {",
  "            age = newAge;",
  "        }",
  "    }",
  "",
  "    int getAge() const {",
  "        return age;",
  "    }",
  "};",
  "```",
  "",
  "## Как читать код",
  "",
  "`private` закрывает поле `age`. Снаружи нельзя написать `student.age = -5`. Метод `setAge` принимает новое значение и решает, можно ли его сохранить. Метод `getAge` возвращает возраст. Слово `const` после `getAge()` означает, что метод не меняет объект.",
  "",
  "`explicit` используется у конструктора с одним параметром, чтобы запретить неявное создание объекта из простого значения. Это защита от странного кода, который компилятор мог бы принять слишком свободно.",
  "",
  "## Как написать самому",
  "",
  "1. Сначала выпишите поля класса.",
  "2. Поместите поля в `private`.",
  "3. Для чтения поля добавьте геттер.",
  "4. Для изменения поля добавьте сеттер.",
  "5. В сеттере проверьте значение.",
  "6. Если поле нельзя менять после создания, не пишите сеттер.",
  "7. Если конструктор с одним параметром, подумайте про `explicit`.",
  "8. Проверьте, что внешний код не обращается к полям напрямую.",
  "",
  "## Мини-пример",
  "",
  "```cpp",
  "class Wallet {",
  "private:",
  "    double money;",
  "",
  "public:",
  "    void setMoney(double newMoney) {",
  "        if (newMoney >= 0) {",
  "            money = newMoney;",
  "        }",
  "    }",
  "",
  "    double getMoney() const {",
  "        return money;",
  "    }",
  "};",
  "```",
  "",
  "## Пример из задач курса",
  "",
  "```cpp",
  "class Planet {",
  "private:",
  "    string name;",
  "    double radius;",
  "    double mass;",
  "",
  "public:",
  "    string getName() const { return name; }",
  "    double getRadius() const { return radius; }",
  "",
  "    void setRadius(double newRadius) {",
  "        if (newRadius > 0) {",
  "            radius = newRadius;",
  "        }",
  "    }",
  "};",
  "```",
  "",
  "Сортировка планет снаружи должна использовать геттеры. Внешний код сравнивает `left.getRadius()` и `right.getRadius()`, не трогая private-поле напрямую.",
  "",
  "## Плохой пример",
  "",
  "```cpp",
  "class Student {",
  "public:",
  "    int age;",
  "};",
  "",
  "int main() {",
  "    Student student;",
  "    student.age = -100;",
  "}",
  "```",
  "",
  "Такой класс не защищает себя. Любой код может записать неправильный возраст.",
  "",
  "## Исправленный пример",
  "",
  "```cpp",
  "class Student {",
  "private:",
  "    int age;",
  "",
  "public:",
  "    void setAge(int newAge) {",
  "        if (newAge >= 0) {",
  "            age = newAge;",
  "        }",
  "    }",
  "",
  "    int getAge() const {",
  "        return age;",
  "    }",
  "};",
  "```",
  "",
  "Теперь неверное значение не проходит через сеттер.",
  "",
  "## Разбор задачи Planet 6.1",
  "",
  "### Сущность",
  "Сущность — планета. У неё есть имя, радиус, масса, расстояние до Солнца или другие поля из условия.",
  "",
  "### Почему поля private",
  "Радиус, масса и расстояние не должны становиться отрицательными. Если оставить поля public, внешний код сможет записать любое значение.",
  "",
  "### Какие геттеры нужны",
  "Геттер нужен для каждого поля, по которому нужно выводить или сортировать. Например `getName`, `getRadius`, `getMass`.",
  "",
  "### Какие сеттеры нужны",
  "Сеттер нужен для редактируемых полей. В сеттере проверьте, что числовое значение больше нуля.",
  "",
  "### Как сортировать",
  "Пользователь выбирает поле сортировки. Код вызывает `sort`, а функция сравнения использует нужный геттер.",
  "",
  "## Связь с Company",
  "",
  "В `Company` инкапсуляция ещё важнее: геттер может не просто вернуть поле, а запретить доступ. Например государственная компания не показывает доходы и расходы. Это правило должно жить внутри класса, а не быть разбросано по меню.",
  "",
  "## Зачем нужен explicit",
  "",
  "`explicit` пишут перед конструктором с одним параметром, чтобы C++ не создавал объект неявно там, где вы этого не ожидали.",
  "",
  "```cpp",
  "class Planet {",
  "private:",
  "    string name;",
  "",
  "public:",
  "    explicit Planet(string name) {",
  "        this->name = name;",
  "    }",
  "};",
  "```",
  "",
  "Без `explicit` компилятор может попытаться превратить строку в объект `Planet` автоматически. Для новичка это выглядит странно: функция ждёт планету, а ей случайно передали строку.",
  "",
  "```cpp",
  "void printPlanet(Planet planet) {",
  "    // вывод планеты",
  "}",
  "",
  "printPlanet(\"Mars\"); // без explicit компилятор может создать Planet из строки",
  "```",
  "",
  "С `explicit` такой неявный вызов запрещён. Если нужен объект, его нужно создать явно: `printPlanet(Planet(\"Mars\"));`. Это делает код понятнее и защищает от случайных преобразований.",
  "",
  "## Частые ошибки",
  "",
  "1. Делают поля public и называют это инкапсуляцией.",
  "2. Пишут геттеры и сеттеры, но всё равно обращаются к полям напрямую.",
  "3. Сеттер принимает любое значение без проверки.",
  "4. Делают сеттер для поля, которое нельзя менять, например для id.",
  "5. Забывают `const` у геттера.",
  "6. Пишут `explicit`, но не понимают, какой неявный вызов он запрещает.",
  "7. Проверки доступа размещают только в меню, а не внутри класса.",
  "",
  "## Мини-проверка",
  "",
  "1. Что защищает инкапсуляция?",
  "2. Почему public-поля опасны?",
  "3. Чем геттер отличается от сеттера?",
  "4. Где должна быть проверка возраста?",
  "5. Почему сортировка должна использовать геттеры?",
  "6. Когда поле не должно иметь сеттер?",
  "7. Зачем нужен `explicit`?",
].join("\n");
