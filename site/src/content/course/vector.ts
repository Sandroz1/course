export const vectorContent = [
  "## Простое объяснение",
  "",
  "`vector` — это список элементов одного типа, который умеет менять размер во время работы программы. Его часто называют динамическим массивом: он похож на массив, потому что элементы идут по порядку и доступны по индексу, но в отличие от обычного массива может расти и уменьшаться.",
  "",
  "Обычный массив неудобен, когда вы заранее не знаете, сколько будет машин, плиток, рабочих или компаний. Если создать массив на 5 элементов, шестой добавить уже нельзя без ручной переделки. `vector` решает эту проблему: можно добавлять элементы через `push_back`, удалять через `erase`, проверять размер через `size` и узнавать, пуст ли список, через `empty`.",
  "",
  "## Зачем это нужно",
  "",
  "Большие задачи курса почти всегда требуют список объектов. В задаче `Plate` нужен список плиток. В `Worker` — список работников. В `Company` — список компаний. Меню пользователя тоже обычно работает со списком: показать все элементы, добавить новый, изменить выбранный, удалить выбранный.",
  "",
  "## Когда использовать",
  "",
  "- Количество элементов заранее неизвестно.",
  "- Нужно добавлять элементы во время работы программы.",
  "- Нужно удалять элементы.",
  "- Нужно пройтись циклом по списку.",
  "- Нужно хранить объекты: `vector<Car>`, `vector<Worker>`, `vector<Company>`.",
  "- Нужно сделать меню для набора объектов.",
  "",
  "## Синтаксис",
  "",
  "```cpp",
  "#include <vector>",
  "",
  "vector<int> numbers;",
  "vector<string> names;",
  "vector<Car> cars;",
  "",
  "cars.push_back(car);",
  "",
  "if (!cars.empty()) {",
  "    cout << cars.size();",
  "}",
  "",
  "cars.erase(cars.begin() + index - 1);",
  "```",
  "",
  "## Как читать код",
  "",
  "`vector<int>` читается как список чисел `int`. `vector<string>` — список строк. `vector<Car>` — список объектов класса `Car`. Имя после типа — это переменная списка: `cars`.",
  "",
  "`push_back(car)` добавляет объект `car` в конец списка. `size()` возвращает количество элементов. `empty()` отвечает, пустой ли список. `begin()` указывает на начало списка. `erase(cars.begin() + index - 1)` удаляет элемент, но только если индекс заранее проверен.",
  "",
  "Индексы в C++ начинаются с нуля. Первый элемент имеет индекс `0`, второй — `1`, третий — `2`. Пользователь обычно вводит номер с единицы, поэтому номер нужно перевести в индекс: `index - 1`.",
  "",
  "## Как написать самому",
  "",
  "1. Подключите `<vector>`.",
  "2. Создайте `vector` нужного типа.",
  "3. Сделайте ввод одного объекта.",
  "4. Добавьте объект в список через `push_back`.",
  "5. Для вывода пройдитесь циклом от `0` до `size()`.",
  "6. Перед удалением проверьте, что список не пуст.",
  "7. Проверьте, что номер пользователя от `1` до `size()`.",
  "8. Удаляйте через `erase(begin() + number - 1)`.",
  "",
  "## Мини-пример",
  "",
  "```cpp",
  "vector<int> numbers;",
  "",
  "numbers.push_back(10);",
  "numbers.push_back(20);",
  "",
  "for (int i = 0; i < static_cast<int>(numbers.size()); i++) {",
  "    cout << i + 1 << \". \" << numbers[i] << endl;",
  "}",
  "```",
  "",
  "## Пример из задач курса",
  "",
  "```cpp",
  "vector<Worker> workers;",
  "",
  "Worker worker;",
  "worker.input();",
  "workers.push_back(worker);",
  "",
  "int number;",
  "cin >> number;",
  "",
  "if (number >= 1 && number <= static_cast<int>(workers.size())) {",
  "    workers.erase(workers.begin() + number - 1);",
  "}",
  "```",
  "",
  "Здесь `workers` хранит несколько работников. Пользователь вводит номер, но в `vector` нужен индекс, поэтому используется `number - 1`.",
  "",
  "## Плохой пример",
  "",
  "```cpp",
  "int number;",
  "cin >> number;",
  "",
  "workers.erase(workers.begin() + number);",
  "```",
  "",
  "Проблемы две: номер пользователя не переведён в индекс и нет проверки границ. Если пользователь введёт слишком большое число, программа может упасть.",
  "",
  "## Исправленный пример",
  "",
  "```cpp",
  "int number;",
  "cin >> number;",
  "",
  "if (number >= 1 && number <= static_cast<int>(workers.size())) {",
  "    workers.erase(workers.begin() + number - 1);",
  "} else {",
  "    cout << \"Неверный номер\" << endl;",
  "}",
  "```",
  "",
  "## Разбор задач Plate, Worker, Company",
  "",
  "### Plate",
  "В задаче `Plate` список нужен, потому что плиток несколько: заранее созданные плитки плюс новые, которые добавляет пользователь. Подходит `vector<Plate> plates;`. Добавление делается через `plates.push_back(plate)`, удаление — через проверенный номер.",
  "",
  "### Worker",
  "В задаче `Worker` список работников должен поддерживать вывод, редактирование, добавление и удаление. Здесь `vector<Worker>` особенно удобен: меню выбирает действие, а код работает с одним контейнером.",
  "",
  "### Company",
  "В задаче `Company` тоже нужен список объектов: несколько компаний с разным типом доступа. `vector<Company>` хранит сами объекты, а при выводе каждый объект решает через геттеры, какие данные можно показать.",
  "",
  "## Как использовать vector в меню",
  "",
  "Типовая схема меню такая: цикл `while`, переменная `choice`, команда `1` для вывода, `2` для добавления, `3` для удаления, `0` для выхода. В каждом пункте меню работайте с `vector`, но не забывайте проверять пустой список и номер пользователя.",
  "",
  "## Частые ошибки",
  "",
  "1. Забывают `#include <vector>`.",
  "2. Путают тип элемента и тип списка: пишут `Car cars;` вместо `vector<Car> cars;`.",
  "3. Используют номер пользователя как индекс без `- 1`.",
  "4. Удаляют элемент без проверки границ.",
  "5. Вызывают `erase` у пустого списка.",
  "6. Пишут цикл `i <= size()`, из-за чего выходят за границы.",
  "7. Забывают, что `size()` возвращает беззнаковый тип, и получают предупреждения при сравнении с `int`.",
  "8. Хранят много отдельных переменных вместо одного `vector`.",
  "",
  "## Мини-проверка",
  "",
  "1. Чем `vector` удобнее обычного массива?",
  "2. Что означает `vector<Car>`?",
  "3. Что делает `push_back`?",
  "4. Почему первый индекс равен `0`?",
  "5. Почему при удалении по номеру пользователя нужен `number - 1`?",
  "6. Что нужно проверить перед `erase`?",
  "7. Когда использовать `empty()`?",
  "",
  "## Задачи после темы",
  "",
  "- `practice/08_vector/ex01_vector_int_push_back.cpp` — добавить числа в список.",
  "- `practice/08_vector/ex02_vector_print_by_index.cpp` — вывести элементы по индексам.",
  "- `practice/08_vector/ex03_vector_delete_by_number.cpp` — удалить по номеру пользователя.",
  "- `practice/08_vector/ex04_vector_empty_check.cpp` — проверить пустой список.",
  "- `practice/08_vector/ex05_vector_objects.cpp` — хранить объекты.",
  "- `practice/08_vector/ex06_vector_sort.cpp` — отсортировать список.",
  "- `practice/08_vector/ex07_vector_menu.cpp` — сделать меню на `vector`.",
  "- `practice/big_tasks/task5_2_worker.cpp` — большая задача со списком работников.",
].join("\n");
