# Stage 1: use this HTTP server before the certificate exists.
# Create the webroot first:
#   sudo mkdir -p /var/www/certbot
#
# After certbot creates the certificate, uncomment the HTTPS server blocks below
# and reload host Nginx.

server {
    listen 80;
    server_name uchicode.ru www.uchicode.ru;

    client_max_body_size 20M;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 5s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# Stage 2: uncomment after Let's Encrypt files exist.
#
# server {
#     listen 443 ssl http2;
#     server_name www.uchicode.ru;
#
#     ssl_certificate /etc/letsencrypt/live/uchicode.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/uchicode.ru/privkey.pem;
#
#     return 301 https://uchicode.ru$request_uri;
# }
#
# server {
#     listen 443 ssl http2;
#     server_name uchicode.ru;
#
#     ssl_certificate /etc/letsencrypt/live/uchicode.ru/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/uchicode.ru/privkey.pem;
#
#     client_max_body_size 20M;
#
#     add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#     add_header X-Content-Type-Options "nosniff" always;
#     add_header X-Frame-Options "DENY" always;
#     add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#     location / {
#         proxy_pass http://127.0.0.1:8080;
#         proxy_http_version 1.1;
#         proxy_set_header Connection "";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_connect_timeout 5s;
#         proxy_send_timeout 60s;
#         proxy_read_timeout 60s;
#     }
# }
